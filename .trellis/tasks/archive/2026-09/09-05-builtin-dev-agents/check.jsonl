{"file": ".trellis/spec/guides/cross-layer-thinking-guide.md", "reason": "Check that the autonomy rule the server enforces and the prompt the agent reads cannot drift, and that a new field added on the row surfaces through response, WS broadcast, schema and UI."}
{"file": ".trellis/spec/core/frontend/index.md", "reason": "New wire types and API methods land in packages/core: verify parseWithFallback usage, that enum-shaped fields stay widened at the boundary, and that no store/localStorage/process.env boundary is crossed."}
{"file": ".trellis/spec/views/frontend/index.md", "reason": "The creation flow and the squad modal live in packages/views: verify no next/* or react-router-dom import, navigation goes through the adapter, and design tokens rather than hardcoded colors."}
